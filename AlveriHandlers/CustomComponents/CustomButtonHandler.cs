using System;
using Microsoft.Maui.Handlers;

namespace AlveriHandlers.CustomComponents
{
    partial class CustomButtonHandler
    {

        //public static IPropertyMapper<Button, CustomButtonHandler> PropertyMapper = new PropertyMapper<Button, CustomButtonHandler>(ViewHandler.ViewMapper)
        //{
        //    [nameof(Button.Text)] = MapText
        //};
        //public static CommandMapper<Button, CustomButton> CommandMapper = new(ViewCommandMapper)
        //{
        //    [nameof(Button.UpdateText)] = MapUpdateText
        //};
        //public CustomButtonHandler(IPropertyMapper mapper, CommandMapper commandMapper = null) : base(mapper, commandMapper)
        //{
        //}

        //public static IPropertyMapper<CustomButton, CustomButtonHandler> PropertyMapper = new PropertyMapper<CustomButton, CustomButtonHandler>(ViewHandler.ViewMapper)
        //{
        //    [nameof(CustomButton.Text)] = MapText
        //};

        //public static CommandMapper<CustomButton, CustomButtonHandler> CommandMapper = new(ViewCommandMapper)
        //{
        //    //[nameof(CustomButton.UpdateStatus)] = MapUpdateStatus
        //};

        //public CustomButtonHandler() : base(PropertyMapper)
        //{
        //}
    }
}

